{
  "name": "solace-readiness-checklist",
  "version": "2.0.0",
  "private": true,
  "description": "Builds the Solace Ops Readiness Checklist (xlsx) from gather-diagnostics bundles, CLI transcripts and current-config exports.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test \"test/**/*.test.js\"",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
